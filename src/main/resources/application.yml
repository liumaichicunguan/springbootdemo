spring:
  profiles:
    active: dev

#  datasource:
#    url: jdbc:mysql://localhost:3306/test?useUnicode=true&charaterEncoding=utf-8
#    username: root
#    password:
#    driver-class-name: com.mysql.jdbc.Driver

  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    password:
    pool:
      max-active: 8
      max-wait: -1
      max-idle: 8
      min-idle: 0
    timeout: 0

mybatis:
  type-aliases-package: com.zdjk.demo.entity
  mapper-locations: classpath*:mapper/*/*.xml

